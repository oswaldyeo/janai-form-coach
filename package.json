{
  "name": "janai-form-coach",
  "version": "2.6.3",
  "private": true,
  "type": "module",
  "description": "On-device strength tracker with an optional camera form coach. Local pose inference, deterministic rep/volume/PR engine, no data upload.",
  "scripts": {
    "test": "node --test",
    "serve": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
